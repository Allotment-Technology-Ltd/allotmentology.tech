# Neon Postgres — create a project at https://neon.tech and paste the connection string.
DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/neondb?sslmode=require"

# Neon Auth — REQUIRED for local dev: Neon Console → your project → Branch → Auth → Configuration.
# Copy the full “Auth URL” (must be a real hostname, not a placeholder).
# Example shape: https://ep-xxx.neonauth.eu-west-2.aws.neon.tech/neondb/auth
# Do not leave a fake URL — DNS errors like ENOTFOUND mean this value was not replaced.
NEON_AUTH_BASE_URL=""

# Optional — Phase 8 AI layer (OpenAI-compatible chat completions)
# AI_PROVIDER="openai-compatible"   # or "restormel-keys"
# AI_API_KEY="sk-..."
# AI_BASE_URL="https://api.openai.com/v1"
# AI_MODEL="gpt-4o-mini"
#
# Optional — Restormel Keys adapter
# RESTORMEL_KEYS_API_KEY="rk_..."
# RESTORMEL_KEYS_BASE_URL="https://api.restormel.dev/v1"
# RESTORMEL_KEYS_MODEL="restormel-writer-v1"
#
# Optional — override canonical BYOK provider/model catalog (default: Restormel dashboard feed).
# RESTORMEL_KEYS_CATALOG_URL="https://restormel.dev/keys/dashboard/api/catalog"
#
# Optional — extra encryption for per-user API keys saved in Settings → BYOK & AI keys.
# If unset, keys are stored in Neon like other app data (use DB access control + Neon features).
# If set (8+ characters), values are encrypted in the app before insert. Keep the same value across deploys.
# BYOK_ENCRYPTION_KEY=""
#
# Optional — Tavily web search for "Discover funding" (Mitchell pulls public programmes into opportunities).
# Get a key at https://tavily.com — separate from your LLM provider key.
# TAVILY_API_KEY=""
